// For partial test on features that are still being implemented

const TEST: int32 = 127;

type Str = string;
type Uint = uint32;

struct User {
    name: Str,
    age: Uint,
}

func test(user: User) {
    println("Hello {user.name}");
}