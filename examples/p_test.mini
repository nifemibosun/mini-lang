// For partial test on features that are still being implemented

const TEST: int32 = 127;

type Str = string;
type Uint = uint32;

struct User {
    name: Str,
    age: Uint,
}

func test(user: User) {
    let mut x: int32 = 10;
}

construct User {
    func new(name: Str, age: Uint): User {
        return name;
    } 
}

//enum Test {
//    User(User),
//    Admin(User),
//}