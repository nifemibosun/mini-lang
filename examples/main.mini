import "std.mini";

fun greet(name: string): string {
    return `Hello {name}`;
}

fun main() {
    let name: string = input("Enter your name: \n");
    let greeting = greet(name);

    print(greet)
}